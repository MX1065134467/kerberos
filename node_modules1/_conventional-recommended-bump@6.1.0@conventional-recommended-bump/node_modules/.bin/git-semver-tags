#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_git-semver-tags@4.1.1@git-semver-tags/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_git-semver-tags@4.1.1@git-semver-tags/cli.js" "$@"
  ret=$?
fi
exit $ret
