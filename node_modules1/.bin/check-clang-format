#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_clang-format@1.6.0@clang-format/bin/check-clang-format.js" "$@"
  ret=$?
else 
  node  "$basedir/../_clang-format@1.6.0@clang-format/bin/check-clang-format.js" "$@"
  ret=$?
fi
exit $ret
