#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/python" ]; then
  "$basedir/python"  "$basedir/../_clang-format@1.6.0@clang-format/bin/git-clang-format" "$@"
  ret=$?
else 
  python  "$basedir/../_clang-format@1.6.0@clang-format/bin/git-clang-format" "$@"
  ret=$?
fi
exit $ret
